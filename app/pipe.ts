//import { Pipe, PipeTransform } from '@angular/core';

//@Pipe({ name: 'addressByClient' })
//export class AddressPipe implements PipeTransform {
//    transform(addresses: any[], customerId: number) {
//        if (addresses == null) {
//            return null;
//        }
//        return addresses.filter(res => res.CustomerId == customerId);
//    }
//}